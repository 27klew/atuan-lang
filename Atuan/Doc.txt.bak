The Language Atuan
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Atuan==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.




BoolLiteral literals are recognized by the regular expression
`````{"True"} | {"False"}`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Atuan are the following:
  | ``Bool`` | ``Int`` | ``data`` | ``else``
  | ``if`` | ``in`` | ``let`` | ``match``
  | ``then`` | ``where`` | ``with`` |

The symbols used in Atuan are the following:
  | = | ( | ) | ::
  | ; | { | } | (lambda
  | => | [ | ] | ,
  | - | ! | && | ||
  | >>> | + | * | /
  | % | < | <= | >
  | >= | == | != | []
  | : | -> |  |

===Comments===
Single-line comments begin with --.There are no multiple-line comments in the grammar.

==The syntactic structure of Atuan==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[Top]//
  | //Top// | -> | //Def//
  |  |  **|**  | //TypeDef//
  | //Def// | -> | //Ident// //[OTIdent]// //OptTypeAnnot// ``=`` //Expr//
  | //TIdent// | -> | ``(`` //Ident// //TypeAnnot// ``)``
  | //OTIdent// | -> | //TIdent//
  |  |  **|**  | //Ident//
  | //TypeDef// | -> | ``data`` //Ident// //[TVar]// ``where`` //[Constr]//
  | //TVar// | -> | //Ident//
  | //[TVar]// | -> | **eps**
  |  |  **|**  | //TVar// //[TVar]//
  | //Constr// | -> | //Ident// //TypeAnnot//
  | //[Constr]// | -> | //Constr//
  |  |  **|**  | //Constr// //[Constr]//
  | //TypeAnnot// | -> | ``::`` //Type//
  | //OptTypeAnnot// | -> | //TypeAnnot//
  |  |  **|**  | **eps**
  | //[Top]// | -> | //Top// ``;``
  |  |  **|**  | //Top// ``;`` //[Top]//
  | //[Ident]// | -> | //Ident//
  |  |  **|**  | //Ident// //[Ident]//
  | //[OTIdent]// | -> | **eps**
  |  |  **|**  | //OTIdent// //[OTIdent]//
  | //Block// | -> | ``{`` //Expr// ``}``
  | //Lambda// | -> | ``(lambda`` //[OTIdent]// //OptTypeAnnot// ``=>`` //Expr// ``)``
  | //Expr9// | -> | //Ident//
  |  |  **|**  | //Integer//
  |  |  **|**  | //BoolLiteral//
  |  |  **|**  | ``[`` //[Val]// ``]``
  |  |  **|**  | ``(`` //Expr// ``)``
  | //Expr8// | -> | //Lambda//
  |  |  **|**  | //Expr9//
  |  |  **|**  | ``(`` //Expr9// ``:`` //Expr9// ``)``
  | //Val// | -> | //Expr9//
  | //[Val]// | -> | **eps**
  |  |  **|**  | //Val//
  |  |  **|**  | //Val// ``,`` //[Val]//
  | //Expr7// | -> | //Expr9// //[Expr9]//
  |  |  **|**  | //Expr8//
  | //Expr6// | -> | ``-`` //Expr7//
  |  |  **|**  | ``!`` //Expr7//
  |  |  **|**  | //Expr7//
  | //Expr5// | -> | //Expr5// //MulOp// //Expr6//
  |  |  **|**  | //Expr6//
  | //Expr4// | -> | //Expr4// //AddOp// //Expr5//
  |  |  **|**  | //Expr5//
  | //Expr3// | -> | //Expr3// //RelOp// //Expr4//
  |  |  **|**  | //Expr4//
  | //Expr2// | -> | //Expr3// ``&&`` //Expr2//
  |  |  **|**  | //Expr3//
  | //Expr1// | -> | //Expr2// ``||`` //Expr1//
  |  |  **|**  | ``match`` //Ident// ``with`` //[PatternBranch]//
  |  |  **|**  | ``if`` //Expr2// ``then`` //Expr2// ``else`` //Expr2//
  |  |  **|**  | ``let`` //Def// ``in`` //Expr2//
  |  |  **|**  | //Expr2//
  | //PatternBranch// | -> | ``(`` //Pattern// ``)`` ``>>>`` //Expr2//
  | //[PatternBranch]// | -> | //PatternBranch//
  |  |  **|**  | //PatternBranch// ``,`` //[PatternBranch]//
  | //Expr// | -> | //Expr1//
  | //[Expr]// | -> | **eps**
  |  |  **|**  | //Expr// //[Expr]//
  | //[Expr9]// | -> | //Expr9//
  |  |  **|**  | //Expr9// //[Expr9]//
  | //AddOp// | -> | ``+``
  |  |  **|**  | ``-``
  | //MulOp// | -> | ``*``
  |  |  **|**  | ``/``
  |  |  **|**  | ``%``
  | //RelOp// | -> | ``<``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``>``
  |  |  **|**  | ``>=``
  |  |  **|**  | ``==``
  |  |  **|**  | ``!=``
  | //ListPattern// | -> | ``[]``
  |  |  **|**  | //Pattern4// ``:`` //Pattern4//
  | //Pattern5// | -> | //Literal//
  |  |  **|**  | ``(`` //Pattern// ``)``
  | //Pattern4// | -> | //Ident// //[Field]//
  |  |  **|**  | //Pattern5//
  | //Pattern3// | -> | //ListPattern//
  |  |  **|**  | //Pattern4//
  | //Pattern// | -> | //Pattern1//
  | //Pattern1// | -> | //Pattern2//
  | //Pattern2// | -> | //Pattern3//
  | //Field// | -> | //Pattern5//
  |  |  **|**  | //Ident//
  | //[Field]// | -> | **eps**
  |  |  **|**  | //Field// //[Field]//
  | //Literal// | -> | //Integer//
  |  |  **|**  | //BoolLiteral//
  |  |  **|**  | ``[`` //[Literal]// ``]``
  | //[Literal]// | -> | **eps**
  |  |  **|**  | //Literal//
  |  |  **|**  | //Literal// ``,`` //[Literal]//
  | //Type4// | -> | ``Int``
  |  |  **|**  | ``Bool``
  |  |  **|**  | ``[`` //Type// ``]``
  |  |  **|**  | //Ident//
  |  |  **|**  | ``(`` //Type// ``)``
  | //Type3// | -> | ``(`` //Ident// //[Type4]// ``)``
  |  |  **|**  | //Type4//
  | //[Type4]// | -> | //Type4//
  |  |  **|**  | //Type4// //[Type4]//
  | //Type1// | -> | //Type2// ``->`` //Type1//
  |  |  **|**  | //Type2//
  | //Type// | -> | //Type1//
  | //Type2// | -> | //Type3//



%% File generated by the BNF Converter (bnfc 2.9.4.1).
